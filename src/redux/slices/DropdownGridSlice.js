import { createSlice } from "@reduxjs/toolkit";

const DropDownGridSlice = createSlice({
  name: "DropDownGrid",
  initialState: {},
  reducers: {},
});

export const {} = DropDownGridSlice.actions;
export default DropDownGridSlice.reducer;
