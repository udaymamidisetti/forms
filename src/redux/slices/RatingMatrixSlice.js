import { createSlice } from "@reduxjs/toolkit";

const RatingMatrixSlice = createSlice({
  name: "RatingMatrix",
  initialState: {},
  reducers: {},
});

export const {} = RatingMatrixSlice.actions;
export default RatingMatrixSlice.reducer;
