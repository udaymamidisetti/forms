import { createSlice } from "@reduxjs/toolkit";

const PercentageSumSlice = createSlice({
  name: "PercentageSum",
  initialState: {},
  reducers: {},
});

export const {} = PercentageSumSlice.actions;
export default PercentageSumSlice.reducer;
