import { createSlice } from "@reduxjs/toolkit";

const TextFieldGridSlice = createSlice({
  name: "TextFielGrid",
  initialState: {},
  reducers: {},
});

export const {} = TextFieldGridSlice.actions;
export default TextFieldGridSlice.reducer;
