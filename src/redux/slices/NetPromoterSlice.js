import { createSlice } from "@reduxjs/toolkit";

const NetPromoterSlice = createSlice({
  name: "NetPromoter",
  initialState: {},
  reducers: {},
});

export const {} = NetPromoterSlice.actions;
export default NetPromoterSlice.reducer;
